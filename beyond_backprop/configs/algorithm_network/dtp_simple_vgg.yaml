# @package _global_
# This is the default behaviour when using the DTP algorithm with the LeNet architecture.
algorithm:
  feedback_training_iterations: [20, 30, 35, 55, 20]
  noise: [0.4, 0.4, 0.2, 0.2, 0.08]
  b_optim:
    lr: [0.0001, 3.5e-4, 0.008, 0.008, 0.18]
